name: "Arcus CodeQL Configuration"

query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
        - note
  - include:
      problem.severity:
        - error

paths:
  - src
