name: "Arcus CodeQL Configuration"

query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
        - note

paths-ignore:
  - src/Arcus.Tests
  - src/Arcus.Benchmarks
